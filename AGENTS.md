# Project instructions

- Always build the game to `bin\HELLO.exe`.
- Never create a second, alternate, temporary, or test executable.
- If `bin\HELLO.exe` is locked, identify the process holding it. Stop a stale automated test process; if it is the user's interactive game, wait for the user to close it before rebuilding.
